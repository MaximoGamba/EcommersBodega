$kind: http-request
name: Create Category
method: POST
url: "{{baseUrl}}/categories"
order: 3000
auth:
  type: bearer
  credentials:
    token: "{{authToken}}"
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "description": "Vinos Blancos"
    }
